# server.port=8084

# # Docker
# # spring.datasource.url=jdbc:mysql://postgres:5432/embeddings
# # Local
# spring.datasource.url=jdbc:postgres://localhost:5432/embeddings
# spring.datasource.username=postgres
# spring.datasource.password=postgres

# spring.jpa.hibernate.ddl-auto=update

server.port=8084

# Database (PostgreSQL - Corrected Protocol)
spring.datasource.url=${EMBED_DB_URL}
spring.datasource.username=${EMBED_DB_USER}
spring.datasource.password=${EMBED_DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
